---
description: Commit and Push changes automatically
---

// turbo-all

1.  **Add all changes**
    ```powershell
    git add .
    ```

2.  **Commit with message**
    ```powershell
    git commit -m "Update"
    ```
